<h2>The El Link List</h2>

  <table class="table">
    <thead>
      <tr>
        <th>Clicks</th>
        <th>URL</th>
      </tr>
    </thead>
    <tbody>
<% list.forEach(function(e) { %>
      <tr>
        <td><%= e.count %></td>
        <td><%= e.url %></td>
      </tr>
<% }); %>
  </table>
